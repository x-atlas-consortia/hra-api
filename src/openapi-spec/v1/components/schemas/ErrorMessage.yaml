title: Error Message
oneOf:
  - type: string
    title: Human readable description of the error
  - type: object
    required:
      - error
    properties:
      error:
        title: Human readable description of the error
        type: string
