title: Error Message
oneOf:
  - type: string
    title: Error message
    description: Human readable description of the error
  - type: object
    title: Error object
    required:
      - error
    properties:
      error:
        type: string
        description: Human readable description of the error
